const messageOne = prompt('Enter the first line');
const messageTwo = prompt('Enter the second line');
const messageThree = prompt('Enter the third line');
const message = `message one ${messageOne}, message two ${messageTwo}, message three ${messageThree}`;

console.log(message);